<resources>
    <!-- Base application theme. -->
    <style name="Theme.SufiIshq" parent="Theme.SplashScreen">
        <item name="backgroundColor">@color/light_background</item>

        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/light_primary</item>
        <item name="colorPrimaryVariant">@color/light_primary_variant</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/light_secondary</item>
        <item name="colorSecondaryVariant">@color/light_secondary_variant</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor">@color/light_background</item>
        <item name="android:navigationBarColor">@color/light_background</item>
        <item name="android:windowLightStatusBar">true</item>

        <!-- Customize your theme here. -->
        <item name="windowSplashScreenBackground">@color/light_background</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_launcher_foreground_dark</item>
        <item name="postSplashScreenTheme">@style/Theme.MaterialComponents.DayNight.DarkActionBar</item>
    </style>

    <style name="Theme.SufiIshq.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.SufiIshq.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.SufiIshq.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>
